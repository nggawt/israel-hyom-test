import {Outlet} from "react-router-dom";

const WriterShareComponent = () => {
  console.log("%cWriterShareComponent-->>> ", "background: rgba(0,0,280,.5)");
  return <Outlet />;
};

export default WriterShareComponent;
